myAnimeList scraper
-------------------------------------------------------------
In order to make this script run you need to have bs4 installed as it uses BeautifulSoup to scrap the pages, just launch the scrapMyAnimeList.py file and leave it for a moment, it will automatically scrap the entire website by itself
